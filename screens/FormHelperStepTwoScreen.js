import React from 'react';
import { Image, Platform, ScrollView, StyleSheet, Text, TouchableOpacity, View } from 'react-native';
import FormHelperStepTwo from '../components/FormHelper/StepTwo';

export default function FormHelperStepTwoScreen() {
	return <FormHelperStepTwo />;
}

FormHelperStepTwoScreen.navigationOptions = {
	headerShown: null
};
